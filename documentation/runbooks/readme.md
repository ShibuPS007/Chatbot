# Application startup

From root of project  
```uvicorn main:app --reload```


For testing  
``` pytest -v```
## Git commands

### for pushing the local file to remote git repo
```code 
git add {file_name}
git commit -m "initial commit"
git push origin {branch-name}
```